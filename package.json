{
  "name": "replay-rack-sports-card-trade-tracker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsc && node --test tests/*.test.js"
  }
}
